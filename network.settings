# ===== Network Settings =====
# If mode is SERVER or HOST, then ip and port are listening.
# If mode is CLIENT, then ip and port are the target address.
# 'default' IP will use your networks public IP address
# use 'localhost' to host a LAN
mode = SERVER
ip = default
port = 55055
onlineMode = true